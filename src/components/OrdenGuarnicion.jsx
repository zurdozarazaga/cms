import React from 'react'
import { Outlet } from 'react-router';

const OrdenGuarnicion = () => {
  return (
    <div>
      orden de guarinicion
    <Outlet />
    </div>
  )
};

export default OrdenGuarnicion;